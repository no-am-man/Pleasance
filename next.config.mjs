/** @type {import('next').NextConfig} */
const nextConfig = {
    // Your Next.js configuration options can go here.
    // For now, we'll keep it minimal to resolve the build error.
    // The empty object is sufficient for Turbopack to work correctly.
};

export default nextConfig;
