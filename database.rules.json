{
  "rules": {
    ".read": true,
    "users": {
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "lastOnline": {
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
