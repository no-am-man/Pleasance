
{
  "rules": {
    "status": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid || root.child('status').child($uid).child('state').val() === 'offline'"
      }
    }
  }
}
