
{
  "rules": {
    "status": {
      ".read": "auth != null",
      "$uid": {
        ".write": "(auth != null && auth.uid === $uid) || (newData.child('state').val() === 'offline')"
      }
    }
  }
}
