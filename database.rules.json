{
  "rules": {
    "users": {
      ".read": true,
      ".write": false,
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "lastOnline": {
      "$uid": {
         ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
